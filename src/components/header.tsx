import React from 'react';
import { StyledHeader } from './styled/';

function Header() {
    return (
        <StyledHeader>
            JukeBox!
        </StyledHeader>
    );
}

export default Header;
